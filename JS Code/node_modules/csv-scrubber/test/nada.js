'use strict';

const assert = require('assert');
const stringify = require('csv-stringify');

describe('nothing here :(', function() {
  it('is not a real test', function() {
    let asdf = stringify().stringify(['asasd', 'dkdikeis']);
    console.log(asdf);
  });
});